<h1>Products</h1>

<% @page.each do |p| %>
  <p class="title"><%= p.title %></p>
  <p class="body"><%= p.body %></p>
  <%= link_to "Edit Post", edit_page_path(p) %>
  <%= link_to "Show Post", pages_products_path(p) %>
<% end %>